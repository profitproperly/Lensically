name: Lensically Recovery RETIRED
run-name: Lensically Recovery RETIRED · ${{ github.sha }}

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Recovery MCP retired
        run: |
          echo "Lensically Recovery is retired. Lensically v2 owns normal recovery; MCP Controller is the fleet break-glass lane."
