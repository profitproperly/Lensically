# Operating Memory

Read this after `AGENTS.md` at the start of every Lensically chat. Keep entries short, factual, and reusable.

## Global Memory

- Failed: the first execution-library sync combined every source adapter into one compound `UNION ALL` query; D1 returned `too many terms in compound SELECT`, so only static tool and table-manifest sources loaded and every routed action failed closed. Use: preserve complete coverage but execute source adapters in bounded compound-query groups, merge the rows in memory, and regression-test library readiness on the first routed action. Applies when: adding execution-library source adapters or any D1 query with many compound SELECT terms.

- Failed: relying only on a timed execution-library refresh allowed a newly written policy to remain unavailable to the next action, while marking high-frequency attempts, receipts, metrics, and gate results dirty would force a full registry refresh after nearly every call. Use: policy-bearing D1 tables mark `dynamic_sources` dirty through database triggers; the very next admission refreshes them before routing. Failures and execution outcomes are written directly into the library in the same transaction, while high-frequency evidence tables remain on the bounded materialized refresh. Applies when: changing Ops Memory, pre-call routes, workflow requirements, incidents, tool overrides, autonomy/decision policy, map entries, backlog rules, strategy policy, source exclusions/cards, or gates.

- Failed: a routed tool can lazily create a D1 table after policy admission, leaving the materialized table manifest behind the live schema; a timer-only refresh would make the next action fail closed until the interval expired. Use: compare the live `sqlite_master` table count with active `d1_table_manifest` coverage on every admission and immediately rebuild the one-query schema manifest when coverage is lower. Applies when: handlers add tables lazily, migrations deploy, or execution-library admission validates D1 completeness.

- Failed: `intent=startup` had a special gateway branch that called startup directly and returned before the mandatory execution library, making startup the only non-conversational action outside the universal gate. Use: startup resolves through the same map/library/guard/finalization path as every other action; preserve its public `gateway.intent=startup` metadata after routed execution and forbid any startup-specific bypass in release preflight. Applies when: changing startup bootstrap, the one-tool public gateway, initialization, or result assembly.

- Failed: runtime gateway metadata added `execution_library`, but `OperatorRoutedGatewayResult` omitted that field, causing exact-SHA TypeScript validation to fail before tests. Use: update the routed result type in the same change as every gateway metadata field and keep TypeScript validation before any test or deployment workflow. Applies when: adding or changing prepared gateway metadata, execution receipts, map state, or public result assembly.

- Failed: the public intent `run typecheck` resolved to `runMcpTests`, which only executes lightweight MCP configuration checks and does not run TypeScript validation. Use: `run typecheck` must map to the exact-SHA engineering workflow `typecheck` task; keep `runMcpTests` limited to built-in MCP self-check language and never report those checks as TypeScript validation. Active `tool_registry_seed` map entries must refresh aliases, schemas, procedures, and fixed routing contracts when the source contract version changes, while verified discovery entries remain untouched. Applies when: routing testing, validation, release-preflight, deployment, or source-defined map updates through `executeLensicallyIntent`.

- Failed: `prepareOperatorRoutedGatewayCall` compiled and returned the mandatory execution-library receipt, but the public gateway omitted `prepared.execution_library` when constructing `routedGatewayMetadata`; the completed action therefore exposed `execution_library: null` even though policy compilation had occurred. Use: copy the prepared receipt into routed gateway metadata before creating the execution guard, preserve it through finalization, and enforce this handoff in release preflight plus the public-gateway regression. Applies when: changing gateway preparation, routed metadata, execution guards, map finalization, or public result assembly.

- Failed: emitting the last 120 verbose Vitest lines as one GitHub annotation buried the actual failed assertion behind passing-test output and annotation truncation. Use: failure diagnostics must extract only failure markers, assertion messages, expected/received lines, and the final test summary, placing those lines first in a bounded annotation. Do not rerun or change implementation when the available diagnostic contains only passing tests. Applies when: Operator or GPT-memory workflow tests fail and ChatGPT can read annotations but not raw private logs.

- Failed: exposing raw execution-library rule text and raw matched source keys in every routed response leaked unrelated tool names into operational results and expanded payloads even when routing was correct. Use: compile and persist the full policy bundle internally before execution, but return only a compact receipt containing the policy-event ID, source coverage, consulted-source counts, and rule counts. The exact raw sources remain auditable in D1 and must not contaminate the operational result surface. Applies when: attaching execution-library evidence to known, unknown, stale, discovery, failure, or success responses.

- Failed: using exact partial replacements on GitHub Actions YAML after the active route required a complete block replacement caused inherited indentation and invalid workflow syntax. Use: for any YAML workflow change, replace the entire affected block or whole file through a reviewed write session, read the complete result back, and only then allow the triggering commit or dispatch. Never use line-level patching for YAML even when each replacement is exact. Applies when: modifying `.github/workflows/*.yml` conditions, steps, inputs, or shell blocks.

- Failed: composing a broad or verbose `executeLensicallyIntent` request before consulting the compact client-safe route caused OpenAI client preflight to block the action before Lensically received it, immediately repeating a known failure while building the execution library itself. Use: every non-conversational action is admitted through `execution-policy-library-v2` before internal execution; the public request remains only compact objective, intent, and variable inputs, and known operational routes use the shortest established intent alias and typed payload. If client preflight still blocks the request before the Worker, do not resend the same shape; record the exact failure and use the already verified compact alias or independent Recovery route. Applies when: any ChatGPT call is about to invoke `executeLensicallyIntent`, particularly engineering, governance, repository, or recovery work.



- Failed: treating an expected protected-operation authorization block as a routing failure after the owner had already approved the action, then switching to recovery instead of preventing recurrence. Use: include the selected brand and the owner's exact approval in `owner_response` on the first protected call. The dispatcher atomically persists an executing decision and execution event before the handler. Protected scheduler recovery uses compact `runApprovedPostCanary`; once its decision is approved, the next scheduler trigger consumes that decision automatically, activates exactly one post, returns to paused after the attempt, and closes the authorization record. The originating chat is not required to retain or refresh the tool schema. Never make a knowingly blocked preliminary call, force scheduler mode through generic arguments, or use recovery for a healthy main-MCP authorization path. Applies when: scheduler mode, repository deletion, runtime rollback, tool disabling, ownership changes, or another protected operation has just received explicit owner approval.



- Use the global `operational-memory` skill for reusable lessons, repeated slowdowns, and token/usage efficiency.
- Global memory lives at `C:\Users\brian\.codex\OPERATING_MEMORY.md`; project-specific Lensically memory stays in this file.
- Project memory is the default write target for Lensically work. Use global memory only for rules that should apply across unrelated projects and chats.
- Workspace-root startup copies also exist at `C:\Auto-Threads\AGENTS.md` and `C:\Auto-Threads\OPERATING_MEMORY.md` because Codex often starts in `C:\Auto-Threads` before entering `lensically`. Keep root startup guidance aligned when startup/memory rules change.
- Failed: trusting the workspace-root startup copies after ChatGPT-side repo changes. Use: after entering `C:\Auto-Threads\lensically`, read repo `AGENTS.md`/`OPERATING_MEMORY.md`, pull clean `origin/main`, then sync root `C:\Auto-Threads\AGENTS.md` and `C:\Auto-Threads\OPERATING_MEMORY.md` from the repo copies when startup guidance changed. Applies when: ChatGPT/GPT may have edited Lensically through the engineering MCP while Codex was away.
- Failed: giving the model a phonebook, guard, startup tool, recovery tool, or route list to consult while still allowing it to choose the execution tool, route, payload, sequence, or fallback. Use: expose exactly one public MCP tool, `executeLensicallyIntent`, with frozen fields `objective`, `intent`, `inputs`, optional `continuation_id`, optional `incident_id`, and optional `permit`; startup is `intent=startup`. `mandatory-execution-map-v1` selects and enforces the active procedure. Known paths are compulsory. If a mapped path genuinely fails, the entry becomes stale and a signed discovery incident is opened; if no entry exists, an unknown-terrain incident is opened. Discovery attempts are recorded, duplicate same-incident attempts are blocked, and the interrupted objective remains paused until the successful path is automatically promoted, activated, and made mandatory. A second occurrence of the same solved mistake is an execution-map regression. Applies when: any Lensically, repository, client, GitHub, Cloudflare, scheduler, publishing, account, source-card, test, deployment, repair, rollback, smoke-test, or recovery action is attempted.
- Failed: treating direct tools, wrappers, bridges, or Recovery as model-selected fallback choices. Use: the model never chooses among execution routes for known work. `executeLensicallyIntent` resolves the active map entry and selects the internal execution plane. A bridge or Recovery route is valid only when the active entry requires it. If that mapped route fails, the entry is blocked as stale and discovery is allowed only under the resulting signed incident. The verified replacement must supersede the old entry before the objective resumes. Applies when: selecting any MCP, GitHub, Cloudflare, client, scheduler, publishing, account, or recovery route.
- Failed: running `npm run typecheck` in `lensically-worker`; the package has no `typecheck` script. Use: `cd lensically-worker; npx tsc --noEmit`. Applies when: validating Worker TypeScript before commit or deployment.
- Failed: relying on direct local Durable Object namespace reads for protected scheduler MCP regression checks after Cloudflare test storage returned `SQLITE_CANTOPEN`. Use: keep scheduler state verification behind the MCP scheduler health/control route and preserve the paused fallback when local DO storage is unavailable. Applies when: local `cloudflare:test` scheduler checks fail while gateway/operator tests otherwise execute.
- Failed: stopping after server-side MCP `tools/list` showed the frozen one-tool surface while the installed ChatGPT app still cached the old multi-tool action schema. Use: after any public MCP schema-surface change, open Chrome to `https://chatgpt.com/plugins/plugin_asdk_app_6a4d1e89578c81918c36dc7227deb1e2`, choose `Plugin actions` > `Manage`, click `Refresh`, and verify the Settings > Plugins action list shows exactly `executeLensicallyIntent` with required `objective`, `intent`, and `inputs`. Applies when: deploying MCP schema changes, retiring public tools, or changing anything that ChatGPT action discovery might cache.
- Verified: Recovery Worker v1.3.0 is an independent break-glass plane with direct source-defined tools. Use: verify `recoveryHealth`, direct repo read/search/patch, GitHub workflow dispatch, Cloudflare state, and `runMainMcpSmoke` from the Recovery app before retiring or changing its surface. Applies when: main gateway routing, map incidents, or ChatGPT schema cache issues could block Operator self-repair.
- Failed: letting Mandatory Execution Map fuzzy scoring rank every tool description before exact operational classes made basic MCP status/verification and engineering repair language route into account-content procedures such as `submit_candidate_draft`, then demand `brand_key`, `run_id`, and `source_card_id`. Use: deterministic operational intent routing must run before fuzzy scoring for startup/status, runtime-repository alignment, repository inspection, engineering diagnosis/repair, tests, deployment, and post-deployment verification. Applies when: changing `mandatoryExecutionMap.ts` matching, aliases, or tool descriptions.
- Failed: treating a signed discovery permit resubmission as invalid before checking that it matches the same open incident, action intent, incident ID, and normalized inputs. Use: validate the permit round trip first; if no `discovery_tool` is supplied, return `mandatory_execution_map_discovery_tool_required` with `permit_accepted: true` instead of `mandatory_execution_map_discovery_invalid`. Applies when: repairing unknown/stale terrain flow behind `executeLensicallyIntent`.
- Failed: routing normal MCP status phrases such as `check gateway execution health` to `engineeringPrecheck` while that handler returned the full startup payload, causing status calls to time out and leaving unfamiliar health wording with no promotable candidate. Use: keep `engineeringPrecheck` compact and bounded, never include `startup_context`, and add deterministic aliases for status/health/version, runtime-repository alignment, diagnosis, repair, testing, deployment, and verification. Applies when: editing gateway status or Mandatory Execution Map routing.
- Failed: reading `runtime.active_runtime_deployment` from D1 MCP runtime-config snapshots as if it were the Cloudflare Worker deployment ID. Use: source live Worker identity from `operatorRuntimeMetadata(env)` / `CF_VERSION_METADATA` and expose D1 snapshots only as `active_runtime_config_deployment`. Applies when: startup, health, deployment verification, or recovery compares live deployment IDs.
- Failed: relying on the chat transcript, proceed flag, memory, or an owner-facing resume/start-fresh question to reconstruct account workflow state. Also failed: returning token-shaped continuity handles or requiring callers to echo a `continuity_loaded` boolean that can drift from ChatGPT's cached schema. Use: after the exact handshake and explicit Proceed, `confirmOperatorProceed` restores canonical D1 schedule, workflow, production-day claims, and any active review batch. Later account calls use their direct typed tools with `proceed_confirmed=true`; Lensically verifies continuity entirely from server-side state. Mutations reuse the stable operation identity from the capsule, and persistent receipts replay completed operations after interruptions. Applies when: any fresh ChatGPT chat selects Manifest, OPMG, or Vectrix.
- Failed: treating every past-due `approved` scheduled post as ordinary calendar coverage allowed fresh-chat continuity to resume generation while the Lensically UI correctly showed Needs Attention. Use: reconcile overdue approved/posting records into durable `operator_operational_incidents`, expose their real delivery state and recovery action, block new scheduling while any delivery incident remains open, and close an incident only after the scheduled row has authoritative `posted` status plus a nonempty published Threads ID. Applies when: fresh-chat continuity, calendar coverage, scheduled-post inspection, scheduler repair, or deployment follows an interrupted or missed publish.

- Failed: sending account workflow, runtime aliases, long governance narratives, or oversized OpsMemory through generic `arguments` objects triggered ChatGPT client preflight before Lensically could apply its server-side known-path policy. Use: `listMcpTools` and `runEngineeringTool` are engineering/admin bridges only. Account work must use direct typed schemas. Protected decisions require only brand, category, title, decision, and authorized tools; bounded narrative defaults are filled server-side. OpsMemory defaults to title plus fix with schema length limits. Applies when: designing or calling any Lensically MCP tool from ChatGPT.
- Failed: choosing the first source draw without a linked card as the next continuation artifact. Historical owner skips and deleted sources then made a fresh chat rewind behind the latest completed source card. Use: canonical continuity prioritizes an unresolved `operator_review_batches` record; otherwise it inspects calendar coverage and creates the next day-level claim batch. Legacy source-selection dispositions remain historical lineage and are never reconstructed from chat memory. Applies when: fresh chats resume Manifest production after partially completed or retired source draws.
- Failed: interpreting “delete source” as deleting the Saved Pattern row, source card, generations, or analytics. Use: insert a durable `operator_source_exclusions` record and mark unresolved selections/claims skipped while preserving the original pattern and all historical lineage. Only Saved Patterns can be excluded permanently; own-account winners remain valid sources. Distinguish this from a `current_day` skip, which blocks only that production date. Applies when: the owner rejects a source rather than only its generated wording.
- Failed: attaching a new draft and source card to a skipped Manifest review item while leaving the claim’s old source identity and selection fields intact. The review then displayed old source text beside a new draft and would corrupt scheduling/result lineage. Use: `attach_manifest_review_draft` verifies the draft/card/run relationship, permits cross-source attachment only after an explicit source skip/delete, requires the replacement selection to belong to the same production date, blocks duplicate same-day claims, and atomically replaces every claim source field while preserving the old selection’s skipped disposition. Applies when: replacing any numbered Manifest review source after detecting recent reuse or rejecting the source for the current day.
- Failed: passing sparse stored draft strategy objects directly into `schedule_manifest_review_batch` caused D1 to reject missing properties as JavaScript `undefined` after earlier items had already scheduled, producing a partial batch. Use: normalize every stored draft strategy through `normalizeGptPostStrategyInput` before strategy-tag persistence. The regression fixture approves and schedules four Manifest review drafts whose saved strategies contain only `lane_key`, proving the batch completes without undefined bindings. Applies when: scheduling any approved four-post Manifest review batch, especially after revisions or older drafts with incomplete strategy metadata.
- Failed: describing the model as autonomous while routine engineering still required owner proposals and finite tool-call budgets. Use: `operator-autonomy-governance-v2` separates account governance from `operator-engineering-authority-v1`. Manifest content, scheduling, strategy, destructive data actions, ownership changes, and irreversible business decisions remain owner-ratified. Routine repository work, tests, workflow repair, deployments, verification, routing, MCP infrastructure, and universal engineering improvements run with persistent full discretion, outcome-bound scope, and no numerical tool budgets. Pending account decisions do not block engineering. Applies when: fresh chats restore authority or any engineering mutation reaches the dispatcher.
- Failed: arming a repaired scheduler while overdue posts were still globally eligible caused the Durable Object alarm to drain the full queue before a one-post canary could be isolated. Use: MCP 1.7.0 persists `paused`, `canary`, or `normal` control inside the scheduler Durable Object. Missing control defaults to `paused`; Cron and alarm both execute through that same control. Canary requires exactly one scheduled-post ID, selects at most one record through the shared atomic publisher, and returns to `paused` after the attempt. Read scheduler state and audit the exact post ID before normal activation. Worker deployment fails when mode is `normal` and the current overdue count is nonzero. Applies when: repairing, deploying, testing, or reactivating automatic scheduled publishing.
- Failed: deployment smoke could leave the scheduler paused with overdue approved rows, and a later normal-mode change had no transactional way to resolve them safely. Use: MCP v1.25.1 serializes normal activation with the scheduler Durable Object, rejects activation with `scheduler_overdue_recovery_required`, and returns exact overdue IDs plus `retire`/`reschedule` actions. Keep the scheduler paused, audit each row, then call `recoverOverdueScheduledPosts` once with explicit IDs: retire sets `cancelled_at` and permanently removes the row from claims; reschedule requires a future ISO timestamp and clears failed/attempt state. Recovery is transactional and bounded to 25 rows. Activate normal only after the recovery result and a second locked overdue check are empty. Applies when: scheduler state is paused and overdue count is nonzero after a deploy, outage, missed publish, or interrupted canary.
- Failed: `runEngineeringRelease` passed a raw commit SHA as the GitHub `workflow_dispatch.ref`, which GitHub rejects with HTTP 422 because dispatch refs must resolve as a branch or tag. Use: dispatch `lensically-engineering.yml` on the configured branch, pass the verified commit as `release_sha`, checkout that SHA explicitly, and fail before validation or deployment unless `git rev-parse HEAD` equals it. Match release receipts by exact `head_sha` or the short SHA embedded in `run-name`. The dependency-free release preflight enforces the workflow input, checkout, verification, and dispatcher source contract. Applies when: validating or deploying any exact Lensically repository SHA.
- Failed: the Mandatory Execution Map classified `find_must_match_once`, `find_text_must_match`, and `head_changed` as reusable path failures, which blocked the correct repository patch tool even though only the variable input needed correction. Use: deterministic patch-input validation failures remain on the known path; correct the anchor or reload the current head and call the same mapped tool again. During seeding, false-stale incidents carrying those signatures reactivate their original stale entry and close as resolved. Applies when: an exact patch is ambiguous, absent, or based on a stale expected head.
- Failed: auto-arming the scheduler alarm on every request whenever Worker version metadata existed caused test and preview requests on noncanonical hosts to start recurring Durable Object alarms, slowing unrelated integration tests and creating timeouts. Use: `shouldAutoArmScheduledPostAlarm` requires the request hostname to match configured `WORKER_ORIGIN`; Cron and explicit scheduler controls remain unaffected. Regression coverage proves `example.com` cannot auto-arm while the canonical Worker host can. Applies when: changing Worker fetch startup, scheduler initialization, preview routes, or integration-test environments.
- Failed: treating Insights and Post Archive as owner-triggered display surfaces left performance learning dependent on manual refreshes and `get_post_results` calls; including capture time inside `metrics_json` also created duplicate snapshots when metrics had not changed. Use: an hourly cron admits autonomous collection only at 12 AM, 6 AM, 12 PM, and 6 PM America/New_York, fetches the latest 40 posts without paging, upserts archive/cache state, appends only changed numeric metric payloads, links available generation lineage, and marks structurally impossible metrics `valid_for_learning=0` with an anomaly reason. Applies when: collecting, reading, scoring, or learning from Threads post performance.
- Failed: attaching an account's daily follower change to posts published on or near that date created unsupported post-level attribution, while requiring 48/72-hour checkpoints exceeded the useful initial-velocity window and would encourage extra calls for posts outside the newest 40. Use: `performance-evaluator-v2` scores each post only from its own age-matched 6/12/18/24-hour views, likes, reposts, quotes, shares, and replies; 24 hours is final. Preserve the existing newest-40 full-Insights collection and never add targeted older-post calls for evaluator maturity. Aggregate account follower snapshots separately for progress toward the account objective. Never label a post, day, batch, or posting period as a follower-growth driver or winner. Applies when: collection, recent insights, growth context, tag summaries, hypotheses, source selection, generation briefs, experiments, or performance review use follower or post data.

- Failed: bumping `OPERATOR_MCP_VERSION` without updating exact version assertions allowed typecheck and the earlier regression suite to pass on different commits, then blocked deployment. Use: whenever the MCP version changes, search the Operator regression file for the prior exact version, update every startup, initialize, and health assertion on the same final commit, then run the full Operator suite before deployment. Applies when: preparing any Operator MCP release.
- Failed: changing Wrangler cron configuration while leaving the deployment workflow's exact expected-cron set unchanged caused a healthy Worker deployment to be reported as failed after Cloudflare had already installed the new triggers. Use: update `wrangler.jsonc`, scheduled-handler constants, timing tests, and the workflow `expected_crons` guard together; replace the complete YAML verification block with explicit relative indentation and read it back before dispatch. Applies when: adding, removing, or changing any Cloudflare Worker cron.
- Failed: committing each related edit separately, dispatching standalone validation workflows before a deployment workflow that repeated them, manually duplicating cron/version expectations, and polling GitHub from chat turned a small release into a roughly 40-minute operation. Use: MCP v1.10.0 applies up to 20 replacements across 12 files through one atomic commit, runs a dependency-free consistency preflight before installation, derives deployed cron expectations from `wrangler.jsonc`, caches worker and recovery dependencies, cancels superseded same-task runs, reuses successful exact-SHA release receipts, dispatches one complete validate-and-deploy workflow, and performs bounded waiting through `getEngineeringRelease`. Applies when: any normal Lensically repository implementation, validation, or deployment is performed through MCP.


- Failed: treating a scheduled-post wording correction as impossible through Operator MCP even though the Lensically UI already supported it. Use: call universal `edit_scheduled_post` for an approved unpublished post. Omitted fields remain unchanged; date and time must be supplied together for rescheduling; selected-account ownership is enforced; posting/posted records are blocked; linked generation-draft text is synchronized so continuity and review batches show the final scheduled wording. The UI and MCP share one backend update helper. Applies when: the owner changes scheduled text, time, or spoiler settings after scheduling.


- Failed: editing the roughly one-megabyte main Worker through GitHub’s Contents API or repeatedly code-searching it after empty index results. Use: both engineering planes read oversized files through Git blobs and write through Git blob/tree/commit/ref operations. `searchRepoFiles` with an exact file prefix reads that named blob once and searches locally; broader searches keep bounded GitHub code search and path-only fallback. Applies when: repository reads, patches, chunked writes, or source search touch large files.
- Failed: `prepareFullPreflight` recursively loaded oversized account-state, source-candidate, strategy-memory, scheduled-post, and gate payloads only to retain section counts; the nested work exhausted the request and escaped as an upstream 502. Use: build account-state coverage from the active session, query lightweight direct database counts for the remaining preflight sections, persist one compact count-summary admission directly, and return structured phase failures instead of throwing. Verified live with `complete=true`, seven complete sections, and zero blockers. Applies when: any account runs post-handshake full preflight through direct or bridged MCP routes.
- Failed: carrying a changing ordinal like-rank into Manifest source selection, rotating fixed leaderboard blocks, applying cross-day cooldowns, or allowing separate chats to claim the same source for one publishing day. Use: a source qualifies only at 1,000+ verified likes; retain exact metric snapshots but no dynamic rank. Deduplicate by stable Threads post ID with canonical URL fallback. For each production date, persist a uniform random draw without performance weighting and claim sources without replacement through `operator_daily_source_claims`; a claimed identity stays unavailable that day across generation, review, revision, approval, scheduling, publishing, and fresh chats. Cross-day reuse remains allowed. Applies when: building or repairing Manifest calendar production and source selection.
- Failed: making the owner review source cards and one generated post at a time, which consumed the day without maintaining hourly inventory. Use: for Manifest, calendar coverage is the controller. After the owner agrees to fill the earliest incomplete future day, claim four sources, create/reuse and lock source cards, generate, self-reject, submit, and gate silently. Show only four numbered Source / Generated post pairs. Process approve/reject/revise by number; schedule approvals into the earliest open hourly slots and report times only after persistence. Applies when: Manifest production runs in any fresh or continuing chat.
- Failed: relying on conversational context or approval memory to communicate workflow transitions, which made the owner infer whether content was a source card, generated post, scheduling confirmation, or completion report. Use: at every owner-visible transition, state `Completed:`, `Showing now:`, and `Next decision:` briefly; report the completed owner-approved action before displaying the next artifact. This is enforced by the source-controlled universal MCP/startup contract and regression tests; memory is secondary only. Applies when: any Manifest, OPMG, or Vectrix workflow changes owner-visible stages in any fresh or continuing chat.
- Failed: changing Manifest generation to close mimicry without changing owner-facing source-card presentation. Fresh chats then exposed internal `must_preserve`, `may_reuse`, `must_transform`, forbidden-combination, inventory, and pass/fail fields as rigid Must preserve / May reuse / Must change / Cannot repeat matrices. Use: `source-card-owner-presentation-v1` keeps raw persistence and gate fields internal, returns the presentation contract with every source-card create/read response, and requires a natural detailed Manifest review using Original source, Source performance, Why the source works, Audience reward, Adaptation approach, Generation freedom, only genuinely Necessary boundaries, and Recommended direction. A source card remains a build specification rather than a finished post. Applies when: any fresh or continuing chat creates, reads, revises, resumes, or presents a source card.

- Failed: forcing Manifest drafts far away from successful sources, then overcorrecting into near-verbatim rearrangement. The first caused invented scenarios and source drift; the second produced plagiarism-level copying. Use: preserve the source mechanism, strongest structural choices, meaning, tone, and payoff while materially rewriting distinctive surface language. Do not synonym-swap or lightly reorder the source, and do not invent unrelated scenes or premises. Manifest `account_rejection_context` remains compact and enforces explicit owner hard bans. `required_gate_execution_gate` still blocks missing gates. Applies when: Manifest creates source cards, generation runs, repairs, gates, or review batches in any chat.
- Failed: treating an older Manifest hard ban as stronger than the owner’s exact wording instruction in the current draft revision. Use: keep historical bans intact globally, but pass the exact current owner-approved phrase through `draft_analysis.owner_requested_exact_surface` or `draft_analysis.owner_approved_surfaces`; `historical_owner_rejection_gate` ignores only banned tokens contained inside that approved phrase for that one submitted draft. Regression coverage proves the same token still blocks without the current per-draft approval. Applies when: the owner explicitly requests wording that includes a surface previously stored as a hard ban.





## Deployment Credentials

- Do not assume Cloudflare credentials are unavailable because direct `wrangler` lacks `CLOUDFLARE_API_TOKEN`.
- Do not inspect `.dev.vars` with commands that print matching lines; it contains live token-looking secrets. Verify env presence with booleans or key names only.
- The Cloudflare deploy token/account are loaded by `lensically-worker/.cloudflare.deploy.ps1`.
- Use the repo deploy scripts so credentials are sourced:
  - Worker: `cd lensically-worker; npm run deploy:cf`
  - Web: `cd lensically-web; npm run deploy:cf`
- `lensically-web` has no plain `npm run deploy` script; use `deploy:cf`.
- Direct `npx wrangler ...` calls need the same credential loader sourced first.
- Failed: using `https://lensically-web.lensically.workers.dev/<new-static-asset>` as the only post-deploy static asset proof returned 404 for newly uploaded favicon PNGs even though Wrangler uploaded them and production served them. Use: verify web static assets on the production custom domain, e.g. `curl.exe -s -I https://lensically.com/lensically-icon-32.png`, and still check `site.webmanifest` content directly. Applies when: validating Lensically web Cloudflare deploys and favicon/app-icon changes.
- The independent `lensically-recovery-worker/` must remain a separate Cloudflare Worker and must not share the main Worker's runtime, D1 preparation, tool registry, or deployment lifecycle. It uses a frozen source-defined recovery schema and GitHub/Cloudflare control-plane access only; never add account, source-card, generation, Threads, or customer-data tools. Its purpose is to let GPT repair/deploy the main MCP from a phone when the main MCP is unavailable.
- Failed: retiring the separate Lensically Recovery MCP public surface before a replacement was independently exposed and verified removed the break-glass path when the main gateway could not repair itself. Use: keep Recovery as a separate ChatGPT app with direct source-defined tools for `recoveryHealth`, repository status/read/search/patch/write, GitHub workflow dispatch/status, Cloudflare state/telemetry/rollback, and `runMainMcpSmoke`. Do not route Recovery through the main Operator gateway or its Mandatory Execution Map. Applies when: changing `lensically-recovery-worker` public tools or ChatGPT app schemas.
- Failed: clicking Create after only entering the recovery MCP URL left the new app disconnected with `No app actions available yet`. Use: open Advanced OAuth settings, set OAuth client ID `lensically-recovery`, keep token auth method `none`, select `recovery` and `offline_access`, disable OIDC, create the app, click Connect, sign in, then click Refresh to scan the nine actions. Applies when: recreating the Lensically Recovery developer app in ChatGPT.
- For usage efficiency, batch Lensically verification and deploys at the end of a coherent work set. Avoid repeated full worker tests, frontend builds, Chrome GPT schema refreshes, and Cloudflare deploys inside the same larger task unless a focused check/live deploy is needed to diagnose a blocker.
- For long Lensically goals, checkpoint safe milestones with targeted checks plus commit/push. Deploy or refresh the Custom GPT only when the checkpoint is independently useful, required for the next step, or the user explicitly asks to checkpoint usable live state.

## GitHub Credentials

- Root `.github.deploy.ps1` loads `GH_TOKEN`.
- It was fixed on 2026-06-14 after a malformed same-line env assignment caused a PowerShell parser error.
- Do not print secret values while debugging credential loading; verify with booleans or token length only.

## Qwen Direction

- Qwen is not to be used for Lensically project automation or post generation.
- Do not delete local Qwen/llama.cpp files unless explicitly asked.
- Remove or avoid startup/task flows that launch the Qwen worker for Lensically.

## Hermes Direction

- Hermes/GPT-backed generation belongs inside Lensically's normal Create Post and Batch Schedule workflow.
- First priority is wiring Hermes to the data Lensically already has: post archive, top posts, saved patterns, scheduled posts, and selected Threads account context.
- Hold off on heavy quality gates until the generation workflow is usable.

## Custom GPT Growth Direction

- Lensically Operator GPT should evolve from post generation into page-growth optimization. Add a GPT growth context action that exposes follower trend, net daily follower change, best/weak growth days, posts near growth windows, saved patterns, scheduled posts, and strategy memory.
- Advanced loop: GPT tags each scheduled post with pillar/hook/style/experiment metadata, Lensically later compares those tags against follower growth and engagement, then GPT proposes rule changes with sample-size caution. Approved changes should persist as `approved_rule` strategy memory.

## Account Separation

- 2026-07-08 universal-default rule: Lensically is multi-account by default. Before changing account behavior, gates, workflow requirements, wrappers, tool bridges, schemas, or generation policy, decide whether the fix should be universal or scoped. If not obvious, ask the owner. After patching, verify and state whether the change landed universally or only for one account. Do not let Manifest beta-testing create hidden Manifest-only fixes that later break Vectrix or OPMG.
- 2026-07-08 accountability rule: memory-only fixes are not enough when a backend/tool path can enforce the behavior. If an error path is repeatable, patch the code, gate, workflow requirement, or bridge so the same mistake becomes backend-rejected or test-covered. Then record the reusable fix here or in ops memory.
- Batch Schedule presets are account-specific, not user-global. Always pass `threads_user_id` when listing, saving, favoriting, deleting, or internally selecting batch presets.
- Legacy unscoped batch presets can be carryovers from another Threads account and should not appear in account-specific Batch Schedule views.

## Browser/Verification Notes

- 2026-07-13 owner priority: Lensically Operator GPT has no usage-limit optimization requirement analogous to Codex. Slowness is acceptable; mid-workflow 502s, loss of the MCP surface, and unexpected read-only degradation are not. Preserve rich intelligence, context, canonical tools, and write authority. Use server-side aggregation/caching, bounded dependency calls, resumable/idempotent workflows, partial structured responses, and transport isolation instead of removing capability or asking the owner to split work to avoid limits.
- 2026-07-13 primary-agent architecture: Lensically Operator GPT plus its private MCP exists to replace routine Codex use and provide an effectively always-available Lensically agent after ChatGPT model degradation, while preserving limited Codex five-hour/weekly capacity for MCP repair, Chrome-only plugin/app maintenance, and other projects. Do not frame GPT/MCP as a secondary or reduced client. The target is Codex-equivalent intelligence and read/write engineering authority through MCP. A mid-workflow 502 or read-only collapse defeats the product and must be eliminated through transport isolation, resumability, and dependency-specific failure handling rather than capability reduction.
- For all Lensically Chrome/browser automation, capture reusable roadblocks immediately in this file: broken browser APIs, stale DOM ids/selectors, internal scroll containers, login/editor quirks, import/save flows, and the exact working workaround. Do this during the same turn once verified, not only at final handoff.
- Failed: querying production D1 `threads_accounts` with `SELECT id, ...` or `SELECT username, ...` when surfacing Lensically account context. Use: `PRAGMA table_info(threads_accounts)` first when unsure; current production columns are `threads_user_id`, `access_token`, `expires_at`, `created_at`, and `configured_account_id`. Brand labels/usernames come from configured account constants and profile/cache tables, not `threads_accounts`. Applies when: inspecting multi-account/GPT memory state with Wrangler D1 queries.
- Failed: running `npm run test -- --run test/gptMemoryRoutes.spec.ts test/operatorMode.spec.ts --reporter=dot` in one Vitest invocation caused D1 table-reset collisions/timeouts because both worker integration suites mutate shared tables. Use: run `npx tsc --noEmit`, then run `npm run test -- --run test/gptMemoryRoutes.spec.ts --reporter=dot` and `npm run test -- --run test/operatorMode.spec.ts --reporter=dot` as separate commands. Applies when: verifying operator-mode and GPT-memory worker tests together.
- Failed: parallel shell runs of those worker Vitest suites can also drop the Cloudflare Workers pool with `Network connection lost`. Use: run the worker typecheck/test commands sequentially, not through `multi_tool_use.parallel`. Applies when final-verifying worker changes.
- Failed: repeated full or focused `test/operatorMode.spec.ts` runs can abort otherwise passing tests with Workers-pool `WSARecv #10053` / `Network connection lost` on this Windows host, even when suites are sequential; globally stubbing `fetch` increased the number of disconnects and is not a fix. Use: do not change production behavior or globally stub the operator suite for this runner fault; rely on fresh-pool focused regressions plus the full run's passing assertions, and rerun the full suite only after restarting the local Workers runtime/environment. Applies when: Vitest reports only `WorkersTestRunner.updateStackedStorage` disconnects with no application assertion or structured worker error.
- Failed: using PowerShell 7-only `Invoke-WebRequest -SkipHttpErrorCheck` or constructing `System.Net.Http.HttpClient` without loading its assembly on this Windows PowerShell 5 host. Use: use `curl.exe` for simple status/body probes; for an in-memory OAuth exchange, run `Add-Type -AssemblyName System.Net.Http` and `Add-Type -AssemblyName System.Web` before constructing `HttpClient`. Applies when: directly verifying Lensically OAuth and MCP endpoints without printing bearer tokens.
- Failed: `getOperatorStartupContext` called `prepareOperatorMode` repeatedly through two `buildOperatorMcpTools` calls plus `operatorMcpInitializeResult`, exhausting Cloudflare's per-invocation API/subrequest limit and returning structured MCP error `Too many API requests by single Worker invocation`. Use: prepare once through the canonical tool build, derive hidden wrapper names locally, and read the constant MCP version without rebuilding the registry. Applies when: startup/bootstrap code needs registry metadata; do not regenerate the full wrapper registry or initialize payload inside one startup call.
- Failed: MCP `initialize`, `tools/list`, canonical `tools/call`, handshakes, and nested handlers all depended on `prepareOperatorMode`, which performs runtime table creation, indexes, default seeding, and many D1 operations; any D1/preparation limit could collapse the entire GPT surface or become a platform 502. Use: keep the canonical MCP control plane source-defined and D1-independent, query runtime overrides additively with canonical fallback, dispatch canonical tools without a registry query, memoize stateful preparation per Worker environment, and return method failures as HTTP 200 JSON-RPC errors with `surface_available: true`. Applies when: adding tools, migrations, dynamic overrides, startup behavior, or shared MCP middleware; never make discovery or canonical dispatch depend on mutable storage.
- Failed: scaffolding the independent recovery Worker with nonexistent `@cloudflare/workers-types@^4.20260710.0` caused npm `ETARGET` and left `tsc` unavailable. Use: query `npm view @cloudflare/workers-types version` and pin the published line; on 2026-07-13 the working version was `^5.20260713.1`. Applies when: installing or refreshing `lensically-recovery-worker` dependencies.
- Failed: treating Cloudflare `GET /accounts/{account}/workers/scripts/lensically-worker/deployments` result as an array made Recovery report zero deployments even though the request succeeded. Use: read `result.deployments`; the beta versions endpoint returns `result` directly as an array. Applies when: parsing `getCloudflareWorkerState` responses or adding rollback/version diagnostics.
- Recovery app refresh proof uses Recovery OAuth, multi-tool discovery, `recoveryHealth`, direct repository/Cloudflare/GitHub tools, and authenticated `runMainMcpSmoke`. Main Operator schema proof remains separate: initialize, `tools/list` exactly `executeLensicallyIntent`, startup through `intent=startup`, mapped repository/test/deployment/repair intents, direct internal-call rejection, and overdue-safe scheduler control. Applies when: validating MCP and Recovery schemas after v1.25.1 / Recovery v1.3.0.
- Failed: creating MCP operator-mode test fixtures for `manifest_mental` in `test/operatorMode.spec.ts` can return MCP `isError: true` when that account is not available in the isolated test binding. Use: create mutating fixtures with the configured `BRAND_KEY` account, and only check other accounts conditionally when their MCP call resolves. Applies when: testing account-scoped MCP gate/session/source-card behavior.
- Failed: `test/operatorMode.spec.ts` account-specific gate scoping fixture (`mcp_vectrix_fixture_gate`) was unreliable under the isolated Cloudflare test binding; cross-account calls could resolve through shared configured fixtures and the owner-account `list_active_gates` response shape was not stable enough for the assertion. Use: avoid mutating gate-scoping fixture assertions in this suite unless the test seeds isolated users/accounts and verifies the exact response shape first. Applies when: adding MCP gate scoping regressions around `list_active_gates`/`create_or_update_gate`.
- Failed: trying to configure the ChatGPT Developer Mode app with the existing Lensically bearer/API-key auth. ChatGPT's New App form only offers OAuth, No Auth, or Mixed; it does not expose a bearer-token/API-key field. Use: Lensically's MCP OAuth bridge (`/.well-known/oauth-authorization-server`, `/api/operator/oauth/authorize`, `/api/operator/oauth/token`) with client ID `lensically-operator-mode`, token endpoint auth method `none`, default scope `operator_mode`, and Cloudflare secret `LENSICALLY_MCP_ACCESS_TOKEN`. Applies when: creating or repairing the private Lensically Operator Mode ChatGPT app.
- Failed: assuming a deployed MCP `tools/list` change is visible to ChatGPT just because direct OAuth/API smoke returns the new tools. Use: in ChatGPT Settings > Apps > Manage enabled apps > Lensically Operator Mode DEV, click `Refresh`, then verify in a brand-new regular ChatGPT chat that the app can call `listMcpTools` and returns `tools: Array(45)`. Applies when: ChatGPT keeps seeing the old 24-tool Lensically app surface after Worker deploy.
- Failed: assuming advertised Lensically engineering tools are directly callable in ChatGPT just because `listMcpTools` shows them. Use: source-backed admin bridge `runEngineeringTool` with `{ tool_name, arguments }`, or existing-callable fallback `listMcpTools` with `{ execute_tool, arguments }`. After Worker deploy, refresh ChatGPT Settings > Apps > Manage enabled apps > Lensically Operator Mode DEV, then verify in a brand-new regular ChatGPT chat that `runEngineeringTool` executes `getEngineeringAccessState` and `listMcpTools` executes `engineeringPrecheck`; success signal on 2026-07-07 was `total_tools is 69`.
- Failed: using `gh secret set` for Lensically GitHub Actions secrets on this Windows workspace because GitHub CLI is not installed and `winget install GitHub.cli` timed out. Use: install `libsodium-wrappers` in a temp directory, load `.github.deploy.ps1` and `lensically-worker/.cloudflare.deploy.ps1`, fetch `/actions/secrets/public-key` with `GH_TOKEN`, seal `CLOUDFLARE_API_TOKEN` and `CLOUDFLARE_ACCOUNT_ID`, then PUT `/actions/secrets/{name}`. Success signal: MCP `deployBackend` dispatches `.github/workflows/lensically-engineering.yml` and the GitHub Actions run completes `success`.
- Failed: `[System.Security.Cryptography.RandomNumberGenerator]::Fill($bytes)` on this Windows PowerShell runtime while generating `LENSICALLY_MCP_ACCESS_TOKEN`; PowerShell still piped the zero-filled variable into `wrangler secret put`, so immediately replace the secret if this happens. Use: `$rng = [System.Security.Cryptography.RNGCryptoServiceProvider]::new(); try { $rng.GetBytes($bytes) } finally { $rng.Dispose() }` before piping the base64url token into `npx wrangler secret put LENSICALLY_MCP_ACCESS_TOKEN --config .\lensically-worker\wrangler.jsonc`. Applies when: generating Cloudflare secrets from PowerShell in this repo.
- Failed: standalone Playwright/in-app browser first for ChatGPT GPT editor work misses the user's logged-in live ChatGPT state. Use: Chrome extension browser first. Applies when: refreshing Lensically Operator GPT actions or editing the Custom GPT.
- Required Chrome extension setup for Lensically Operator GPT schema refreshes: read `chrome:control-chrome`, initialize `C:\Users\brian\.codex\plugins\cache\openai-bundled\chrome\26.623.101652\scripts\browser-client.mjs` through `mcp__node_repl__js`, call `agent.browsers.get("extension")`, then read `await browser.documentation()` before interacting.
- Failed: declaring Chrome extension tooling unavailable before checking tool discovery and the browser-client path. Use: run tool discovery for `node_repl js` and the Chrome skill/browser-client path before declaring it blocked. Applies when: Chrome extension browser is not already visible as a callable tool.
- Failed: PowerShell `SendKeys` for native browser prompts/UI automation. Use: `computer-use:computer-use` through `computer-use-client.mjs` as the fallback. Applies when: native browser confirm dialogs appear or Chrome extension automation hangs during GPT editor work.
- Failed: passing text to Chrome extension dialog `accept()` or bundling waits/DOM reads after accept can hang/reset the JS session. Use: `const dialog = await tab.getJsDialog(); if (dialog) await dialog.accept();` as a short standalone call. Applies when: Chrome extension JavaScript confirm dialogs appear.
- Failed: repeated Playwright `domSnapshot()` attempts on the ChatGPT GPT editor when it throws `incrementalAriaSnapshot is not a function`. Use: switch immediately to `tab.dom_cua.get_visible_dom()` plus screenshots. Applies when: refreshing the Lensically Operator GPT schema in Chrome.
- Failed: page-level `tab.dom_cua.scroll()` or wheel scrolling for the ChatGPT GPT editor left Configure pane. Use: screenshot to locate the pane scrollbar, drag the visible scrollbar/thumb directly, then inspect `dom_cua` for Actions. Applies when: trying to reveal the Actions section in the Custom GPT Configure pane. In a 1536x650 Chrome viewport, drag near `x~752` from `y~150` toward `y~520`.
- Failed: reusing a `dom_cua` node id from before a scroll, import, delete, or panel transition. Use: re-run `tab.dom_cua.get_visible_dom()` and click only fresh node ids. Applies when: interacting with action gear/settings, import, delete, or update controls in the GPT editor.
- Failed: retrying stale DOM ids after importing a GPT OpenAPI schema from URL. Use: re-run `tab.dom_cua.get_visible_dom()` before clicking `Update`. Applies when: importing or updating the Lensically Operator GPT action schema.
- Failed: ChatGPT Custom GPT editor rejects action schemas with more than 30 operations. Use: trim the GPT-exported OpenAPI schema to 30 advertised operations before importing. Applies when: Lensically worker changes add GPT-facing actions.
- Failed: ChatGPT Custom GPT editor rejects duplicate action domains such as duplicate `api.lensically.com`. Use: update the existing `api.lensically.com` action or delete the accidental duplicate action, then import into the existing action. Applies when: schema import reports duplicate domain.
- Failed: GPT recent insights read path used SQLite `datetime(post_timestamp)` for the day-window filter, which can drop freshly upserted Threads rows when live timestamp formats are parseable by JS but not by SQLite. Use: read account rows first, then filter/sort day-window results with JS `Date.parse()` so the read path matches `refreshGptInsightsFromThreads` stop logic. Applies when: `/api/gpt/insights/recent` shows `posts_seen/posts_upserted > 0` but `insights: []` / `total_count: 0`.
- Success signals: focused `npm test -- --run test/gptMemoryRoutes.spec.ts` should include the day-window pagination test with one `toUTCString()` timestamp and still return the in-window posts.
- Failed: hard-coding July 2026 timestamps in `test/gptMemoryRoutes.spec.ts` fixtures made rolling recent-performance and preflight recent-insights assertions fail once the test clock moved past the 7-day window. Use: current-relative fixture helpers such as `recentPostedFixtureTimes()` / `recentFixtureIso()` for archive, scheduled, saved-pattern, and follower snapshot timestamps when testing rolling windows. Applies when: GPT memory tests assert `recent_insights`, `last_7_days`, or preflight snapshot recent archive sections.
- Failed: testing fetched-id/archive-id mismatch by spying on D1 `env.DB.prepare()` and replacing text in the SQL string does not rewrite bound values, so the endpoint still stores the original fetched id. Use: model GPT insights read regressions through realistic timestamp/date parsing fixtures or explicit helper boundaries instead of SQL-string rewrites for bound params. Applies when: adding worker route tests around D1 prepared statements.
- Failed: Threads live refresh could overwrite nonzero archived metrics with zeros because `upsertThreadsPostsArchive` assigned `views/likes/replies/reposts/quotes/shares/engagement_total = excluded.*`. Use: treat existing archived metrics as a floor with `max(existing, incoming)` in both hydration and archive upsert. Applies when: `/api/gpt/insights/recent` or `/api/threads/posts` refresh touches older posts whose live per-post insights return zeros.
- Pagination note: GPT recent insights does not have a 5-page hard limit. `MAX_THREADS_POST_CURSOR_DEPTH` is 250. `days=3` defaults `max_pages` to 5 because default is `days + 2`; callers can pass higher `max_pages`, and `has_more: false` after 2 pages means Threads returned no `paging.next`/cursor for that pull.
- 2026-07-05 archive audit: production `threads_posts_archive` had 514 rows, 514 distinct `post_id`s, 514 distinct `threads_user_id:post_id` pairs, and 0 duplicate text groups. No duplicate cleanup was needed. It had 84 all-zero metric rows; 70 were recently synced around `2026-07-05 18:38:00/01` and looked like the bad live-refresh overwrite batch. `threads_post_insights_cache` did not have nonzero replacements for those zero archive rows.
- Failed: reading `.cloudflare.deploy.ps1` with `Get-Content -Raw` printed the Cloudflare token. Use: do not open credential loader files; only dot-source `. .\lensically-worker\.cloudflare.deploy.ps1` inside the command that needs credentials, and verify with nonsecret query results. Applies when: running Wrangler/D1 commands from Codex.
- For Lensically Operator GPT schema refreshes, use the Chrome extension browser path first because it has the user's logged-in ChatGPT state. Required setup: read `chrome:control-chrome`, initialize `C:\Users\brian\.codex\plugins\cache\openai-bundled\chrome\26.623.101652\scripts\browser-client.mjs` through `mcp__node_repl__js`, call `agent.browsers.get("extension")`, then read `await browser.documentation()` before interacting. Do not use standalone Playwright as the first path for ChatGPT GPT editor work.
- If Chrome extension tooling appears unavailable, run tool discovery for `node_repl js` and the Chrome skill/browser-client path before declaring it blocked. If it still fails, capture the exact blocker here before final.
- For native browser confirm dialogs or Chrome extension hangs during GPT editor work, use `computer-use:computer-use` through its `computer-use-client.mjs` path as the fallback. Do not use PowerShell `SendKeys` for UI automation; Computer Use is the supported Windows prompt/control surface.
- For Chrome extension JavaScript confirm dialogs, first try `const dialog = await tab.getJsDialog(); if (dialog) await dialog.accept();` as a short standalone call with no bundled waits or follow-up DOM reads. Passing text to `accept()` or bundling waits after accept can hang/reset the JS session.
- This Windows workspace uses `powershell`; `pwsh` is not installed. Do not wrap scripts by piping to `pwsh -Command -`.
- If Browser tooling is unavailable, local production build plus route HTTP checks are acceptable fallback verification, but state the limitation.
- The bundled Playwright package may be incomplete on this Windows box (`playwright-core` missing). Do not waste time repeatedly trying it without checking the dependency path first.
- Lensically frontend profile/avatar images intentionally use some plain `<img>` tags for external Threads CDN URLs. Treat `@next/next/no-img-element` profile-picture warnings as known non-blocking noise unless they become a failing error or the task specifically targets image optimization.
- When refreshing the Lensically Operator GPT schema in Chrome, avoid repeated Playwright `domSnapshot()` attempts on the ChatGPT GPT editor if it throws `incrementalAriaSnapshot is not a function`. Switch immediately to `tab.dom_cua.get_visible_dom()` plus screenshots.
- ChatGPT GPT editor uses an internal Configure-panel scroll. If normal DOM/Playwright scrolling does not reveal Actions, use coordinate scrolling inside the left configure pane, then inspect `dom_cua` for the `api.lensically.com` action gear/settings.
- For ChatGPT GPT editor Configure scrolling, do not use page-level `tab.dom_cua.scroll()` or wheel scrolling for the left Configure pane; it does not move the internal pane reliably. Use a screenshot to locate the pane scrollbar, then drag the visible scrollbar/thumb directly and inspect `dom_cua` for Actions. In the 1536x650 Chrome viewport, the working pattern was dragging the left pane scrollbar near `x≈752` down from `y≈150` toward `y≈520`.
- If a `dom_cua` node id is from before a scroll, import, delete, or panel transition, treat it as stale. Re-run `tab.dom_cua.get_visible_dom()` and click only fresh node ids. Do not retry the old id.
- After importing a GPT OpenAPI schema from URL, DOM node ids often become stale. Re-run `tab.dom_cua.get_visible_dom()` before clicking `Update`; do not retry stale ids.
- Known GPT schema refresh path: open `https://chatgpt.com/gpts/editor/g-6a46c40d41d08191b05eef6e08ab123a`, Configure, scroll left pane to Actions, open the `api.lensically.com` action settings gear, click `Import from URL`, import `https://api.lensically.com/api/gpt/openapi.json`, verify operation ids in the schema text, click fresh `Update`, and look for `View GPT`/`Copy link` confirmation.
- 2026-07-05: GPT insights action changed from hidden 72-hour archive filtering to page-based Threads Insights pulls. `getRecentInsights` now defaults to latest 40 posts; optional `days` keeps fetching 40-post pages until the posted date crosses the requested day window; `max_pages`, `cursor`, and `cursor_depth` control deeper refresh/pagination. After worker deploy, refresh the Lensically Operator GPT schema so these new params are available.
- 2026-07-05: ChatGPT Custom GPT editor rejects action schemas with more than 30 operations and also rejects duplicate action domains. If schema import says duplicate `api.lensically.com`, delete the accidentally-created duplicate action or update the existing action instead; use Computer Use for the native delete confirmation if Chrome extension automation hangs.
- 2026-07-05: Lensically Operator GPT schema refresh succeeded after deleting the accidental duplicate action, trimming OpenAPI to 30 advertised operations, importing `https://api.lensically.com/api/gpt/openapi.json` into the existing `api.lensically.com` action, and clicking `Update`. Success signals were `View GPT` and `Copy link`.
- For Threads mobile save extraction, do not prefer raw `article.innerText` just because it has newlines. It captures action rows/comments/footer text. Preserve multiline post text with bounded post-body fixture tests (`lensically-web/scripts/test-mobile-save-extractor.mjs`) before changing the bookmarklet/extractor.
- For desktop Threads saver extraction, activity/composer UI can be appended inline after the real post text (`SortTopMoreView activity...Reply...Attach mediaAdd a GIFExpand composer`). Keep worker-side saved-pattern sanitizer tests for this shape, not just extension cleanup.
- For iPhone Threads mobile save, the user expects an inline `javascript:(()=>{...})()` bookmarklet pasted as the bookmark URL. Do not give a hosted-script loader bookmarklet (`document.createElement('script').src=...`) unless explicitly requested; that format failed for the user. Use the latest inline format from the `Locate Threads save post extension` thread as the base.

## Usage Cost Notes

- 2026-06-14: Batch Schedule account scoping fix:
  before 5h 49%, weekly 82%; after 5h 42%, weekly 81%; delta 5h -7%, weekly -1%.
  Work included targeted backend/frontend reads, worker/web patches, worker typecheck, worker tests, web lint/build, and worker/web Cloudflare deploys.
