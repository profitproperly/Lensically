# Worker Monolith Refactor Continuation — Superseded

The canonical engineering handoff is now the repository-root file:

`ENGINEERING_CONTINUATION.md`

Fresh chats must call `getEngineeringContinuation`, reconcile with `getRepoStatus`, and follow the single `Current Action` recorded there.

This file is not an implementation authority and must not be expanded with new continuation state.
